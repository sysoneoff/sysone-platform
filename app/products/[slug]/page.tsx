import Link from "next/link";
import { notFound } from "next/navigation";
import { CheckCircle2, ArrowRight, FileText, ShieldCheck } from "lucide-react";
import { products } from "@/data/catalog";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}));}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)return notFound();return <div className="pageWrap"><section className="productDetailHero"><div className="shell productDetailGrid"><div><span className="eyebrow">SYSONE / {product.category.toUpperCase()}</span><h1>{product.name}</h1><p>{product.description}</p><div className="platformRow big">{product.platforms.map(p=><span key={p}>{p}</span>)}</div><div className="heroActions"><Link className="button buttonPrimary buttonLarge" href="/contact"><ArrowRight size={17}/> {product.status==="Available"?"Request access":product.status==="Beta"?"Join beta":"Follow product"}</Link><Link className="button buttonGhost buttonLarge" href="/docs"><FileText size={17}/> Documentation</Link></div></div><div className={`surface productDetailArt accent-${product.accent}`}><div className="detailMonogram">{product.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</div><span>Version {product.version}</span></div></div></section><section className="section"><div className="shell detailColumns"><div><span className="eyebrow">FEATURES</span><h2>Designed to stay useful.</h2></div><div className="detailFeatureList">{product.features.map(f=><div key={f}><CheckCircle2 size={18}/><span><strong>{f}</strong><small>Structured for the SysOne product lifecycle and future updates.</small></span></div>)}</div></div></section><section className="section sectionTint"><div className="shell trustPanel surface"><ShieldCheck/><div><h2>Secure distribution architecture</h2><p>Production downloads are planned around ownership checks, entitlements and short-lived signed links instead of permanent public file URLs.</p></div><Link href="/support" className="textLink">Product support</Link></div></section></div>}
