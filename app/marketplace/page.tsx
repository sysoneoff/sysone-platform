import { products, games } from "@/data/catalog";
import { ProductCard } from "@/components/ProductCard";
import { GameCard } from "@/components/GameCard";
import { Search, SlidersHorizontal, ShoppingBag } from "lucide-react";
export default function MarketplacePage(){return <div className="pageWrap"><section className="pageHero shell marketplaceHero"><span className="eyebrow">MARKETPLACE</span><h1>Software, games and digital products in one store.</h1><p>The marketplace foundation is prepared for free, one-time, subscription and lifetime products, plus licensing, reviews, wishlists, promos and secure downloads.</p><div className="marketSearch surface"><Search size={18}/><input placeholder="Search marketplace..." aria-label="Search marketplace"/><button><SlidersHorizontal size={17}/> Filters</button></div></section><section className="section compactTop"><div className="shell"><div className="marketTabs"><span className="active">All</span><span>Software</span><span>Games</span><span>AI Tools</span><span>Business</span><span>Free</span><span>Premium</span></div><div className="sectionMiniHead"><div><ShoppingBag/><h2>Featured software</h2></div><span>{products.length} products</span></div><div className="cardGrid3">{products.slice(0,3).map(p=><ProductCard key={p.slug} product={p}/>)}</div><div className="sectionMiniHead marketSecond"><div><ShoppingBag/><h2>Featured games</h2></div><span>{games.length} games</span></div><div className="cardGrid3">{games.map(g=><GameCard key={g.slug} game={g}/>)}</div></div></section></div>}
