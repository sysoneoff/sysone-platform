import { caseStudies } from "@/data/catalog";
import { ArrowRight } from "lucide-react";
export default function CaseStudiesPage(){return <div className="pageWrap"><section className="pageHero shell"><span className="eyebrow">CASE STUDIES</span><h1>What changed after the product existed?</h1><p>SysOne case studies are structured around the problem, research, solution, technology and measurable outcome — not a screenshot gallery.</p></section><section className="section compactTop"><div className="shell caseStudyList">{caseStudies.map((c,i)=><article className="surface" key={c.title}><span className="caseNumber">0{i+1}</span><div><span className="eyebrow">{c.metric}</span><h2>{c.title}</h2><p>{c.description}</p><div className="platformRow">{c.tags.map(t=><span key={t}>{t}</span>)}</div></div><button className="roundAction"><ArrowRight/></button></article>)}</div></section></div>}
