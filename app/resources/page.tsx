import { resources } from "@/data/catalog";
import { ArrowUpRight, BookOpen, FileText, Newspaper } from "lucide-react";
export default function ResourcesPage(){return <div className="pageWrap"><section className="pageHero shell"><span className="eyebrow">RESOURCES</span><h1>Learn how SysOne thinks and builds.</h1><p>Guides, articles, product updates, changelogs and technical documentation support both customers and developers.</p></section><section className="section compactTop"><div className="shell resourceGrid">{resources.map((r,i)=><article className="surface resourceCard" key={r.title}><span className="resourceIcon">{i%3===0?<BookOpen/>:i%3===1?<Newspaper/>:<FileText/>}</span><span className="eyebrow">{r.type}</span><h2>{r.title}</h2><p>{r.summary}</p><button className="textButton">Read resource <ArrowUpRight size={15}/></button></article>)}</div></section></div>}
